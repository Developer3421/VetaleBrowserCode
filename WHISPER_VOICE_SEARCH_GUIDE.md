# Голосовий пошук у VetaleBrowser з Whisper.NET

## Огляд

VetaleBrowser тепер використовує **Whisper.NET** для офлайн розпізнавання голосу. Whisper - це модель від OpenAI, яка підтримує **99+ мов** та забезпечує високу точність розпізнавання.

⚡ **Важливо**: Модель Whisper працює локально на вашому ПК, не потребує інтернету та не надсилає дані назовні!

## Переваги Whisper.NET

✅ **Багатомовність**: автоматичне визначення та підтримка 99+ мов  
✅ **Висока точність**: краще розпізнавання порівняно з Vosk  
✅ **Офлайн**: повністю працює без інтернету  
✅ **Локальна обробка**: всі дані залишаються на вашому ПК  
✅ **Підтримка діалектів**: краще розуміння різних акцентів  
✅ **Міжнародна підтримка**: створено OpenAI

## Встановлення

### Варіант 1: Використання вбудованої моделі (Рекомендовано)

VetaleBrowser автоматично використовує модель, яка йде разом з пакетом **Whisper.net.Runtime**.

**Нічого робити не потрібно!** Просто запустіть браузер і використовуйте голосовий пошук.

### Варіант 2: Користувацька локальна модель

Якщо ви хочете використовувати іншу модель (для кращої якості або швидкості):

1. **Завантажте модель** з https://huggingface.co/ggerganov/whisper.cpp/tree/main

2. **Помістіть модель** в одне з місць:
   - `%AppData%\VetaleBrowser\Models\ggml-base.bin`
   - `<папка програми>\Models\whisper\ggml-base.bin`
   - `<папка програми>\VoiceModels\ggml-base.bin`

3. **Готово!** VetaleBrowser автоматично знайде та використає вашу модель.

### Доступні моделі

| Модель | Розмір | Швидкість | Точність | Рекомендація |
|--------|--------|-----------|----------|--------------|
| **ggml-tiny.bin** | 75 MB | ⚡⚡⚡ | ⭐⭐ | Для слабких ПК |
| **ggml-base.bin** | 142 MB | ⚡⚡ | ⭐⭐⭐ | ✅ Вбудовано |
| **ggml-small.bin** | 466 MB | ⚡ | ⭐⭐⭐⭐ | Для кращої точності |
| **ggml-medium.bin** | 1.5 GB | 🐌 | ⭐⭐⭐⭐⭐ | Максимальна якість |

**За замовчуванням**: Використовується вбудована `base` модель.

## Використання

### Голосовий пошук на домашній сторінці

1. Відкрийте VetaleBrowser
2. На домашній сторінці натисніть кнопку мікрофона (🎤)
3. Говоріть свій запит
4. Натисніть кнопку зупинки (⏹️) або чекайте автоматичного зупинення
5. Текст автоматично з'явиться в полі пошуку і виконається пошук

### Підтримувані мови

Whisper автоматично визначає мову. Підтримуються:

- 🇺🇦 Українська
- 🇬🇧 Англійська
- 🇷🇺 Російська
- 🇵🇱 Польська
- 🇩🇪 Німецька
- 🇫🇷 Французька
- 🇪🇸 Іспанська
- 🇮🇹 Італійська
- 🇯🇵 Японська
- 🇨🇳 Китайська
- ...і багато інших (99+ мов)

## Технічні деталі

### Архітектура

```
Мікрофон → NAudio → Буфер аудіо → Whisper.NET → Текст → Пошук
```

### Формат аудіо

- **Частота**: 16 кГц (оптимально для Whisper)
- **Канали**: Моно
- **Формат**: 16-bit PCM
- **Конвертація**: Автоматична в float32 [-1.0, 1.0]

### Процес розпізнавання

1. **Запис**: NAudio записує аудіо з мікрофона
2. **Буферизація**: Аудіо зберігається в пам'яті
3. **Конвертація**: 16-bit PCM → float32 array
4. **Розпізнавання**: Whisper.NET обробляє аудіо асинхронно
5. **Результат**: Текст передається в UI

### Залежності

```xml
<PackageReference Include="Whisper.net" Version="1.9.0" />
<PackageReference Include="Whisper.net.Runtime" Version="1.9.0" />
<PackageReference Include="NAudio" Version="2.2.1" />
```

## Усунення проблем

### Голосовий пошук не працює

**Проблема**: Кнопка мікрофона неактивна або не реагує

**Рішення**:
1. **Перевірте мікрофон**:
   - Підключіть мікрофон до ПК
   - Перевірте налаштування Windows (Параметри → Конфіденційність → Мікрофон)
   - Надайте дозвіл VetaleBrowser на використання мікрофона

2. **Перевірте модель** (якщо використовуєте власну):
   - Переконайтеся, що файл `.bin` існує
   - Перевірте права доступу до файлу
   - Переконайтеся, що файл не пошкоджений

3. **Перегляньте логи**:
   - Запустіть VetaleBrowser в Debug режимі
   - Шукайте повідомлення `[VoiceRecognition]` в консолі

### Модель не знайдена

**Проблема**: "Модель Whisper не знайдена"

**Це не повинно статися!** VetaleBrowser використовує вбудовану модель.

**Якщо все ж таки виникла помилка**:
1. Переконайтеся, що пакет `Whisper.net.Runtime` встановлено
2. Перевстановіть браузер
3. Помістіть `ggml-base.bin` вручну в `%AppData%\VetaleBrowser\Models\`

### Де зберігається модель?

**Вбудована модель**: Знаходиться в пакеті NuGet `Whisper.net.Runtime`, автоматично копіюється при збірці.

**Користувацькі моделі** шукаються в:
1. `%AppData%\VetaleBrowser\Models\ggml-*.bin`
2. `<папка програми>\Models\whisper\ggml-*.bin`
3. `<папка програми>\VoiceModels\ggml-*.bin`
4. `<папка програми>\ggml-*.bin`

### Мікрофон не знайдено

**Проблема**: "No microphone devices found"

**Рішення**:
1. Підключіть мікрофон
2. Перевірте налаштування Windows
3. Надайте дозвіл на використання мікрофона

### Низька швидкість розпізнавання

**Проблема**: Обробка займає багато часу

**Рішення**:
1. Використовуйте модель `ggml-tiny.bin` (швидша)
2. Говоріть коротші фрази
3. Оновіть драйвери аудіо

### Погана якість розпізнавання

**Проблема**: Текст розпізнається неправильно

**Рішення**:
1. Використовуйте модель `ggml-small.bin` або `ggml-medium.bin`
2. Говоріть чітко і повільно
3. Зменшіть фоновий шум
4. Перевірте якість мікрофона

## Порівняння з Vosk

| Характеристика | Vosk | Whisper.NET |
|----------------|------|-------------|
| **Мови** | ~20 | 99+ |
| **Точність** | Середня | Висока |
| **Швидкість** | Швидка | Середня |
| **Розмір моделі** | 40-1500 MB | 75-1500 MB |
| **Авто-визначення мови** | ❌ | ✅ |
| **Підтримка** | Обмежена | Активна (OpenAI) |

## Приклади використання

### Українська мова

🎤 "Відкрий Вікіпедію"  
→ Пошук: "Відкрий Вікіпедію"

🎤 "Який курс долара сьогодні"  
→ Пошук: "Який курс долара сьогодні"

### Англійська мова

🎤 "Search for Python tutorials"  
→ Пошук: "Search for Python tutorials"

🎤 "What's the weather today"  
→ Пошук: "What's the weather today"

### Змішані мови

Whisper автоматично розпізнає мову і може навіть обробляти змішані фрази!

## Розробка

### Інтерфейс IVoiceRecognitionService

```csharp
public interface IVoiceRecognitionService
{
    event EventHandler<string>? TextRecognized;
    event EventHandler<VoiceRecognitionState>? StateChanged;
    event EventHandler<string>? ErrorOccurred;
    
    Task StartListeningAsync();
    void StopListening();
    bool IsAvailable();
    VoiceRecognitionState CurrentState { get; }
}
```

### Приклад використання в коді

```csharp
var voiceService = new WindowsVoiceRecognitionService();

voiceService.TextRecognized += (s, text) => {
    Console.WriteLine($"Розпізнано: {text}");
};

voiceService.StateChanged += (s, state) => {
    Console.WriteLine($"Стан: {state}");
};

if (voiceService.IsAvailable())
{
    await voiceService.StartListeningAsync();
    // Говоріть...
    voiceService.StopListening();
}
```

## Ресурси

- **Офіційний сайт Whisper**: https://openai.com/research/whisper
- **Whisper.NET GitHub**: https://github.com/sandrohanea/whisper.net
- **Моделі GGML**: https://huggingface.co/ggerganov/whisper.cpp
- **Документація NAudio**: https://github.com/naudio/NAudio

## Ліцензія

- **Whisper**: MIT License (OpenAI)
- **Whisper.NET**: MIT License
- **NAudio**: MIT License

## Підтримка

Якщо у вас виникли проблеми:

1. Перевірте, що модель завантажена правильно
2. Переконайтеся, що мікрофон працює
3. Перегляньте логи в Debug консолі
4. Створіть issue на GitHub

---

**VetaleBrowser** - Ваш приватний браузер з голосовим управлінням! 🎤🔍

