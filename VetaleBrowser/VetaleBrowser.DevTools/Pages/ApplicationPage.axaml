<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="VetaleBrowser.VetaleBrowser.DevTools.Pages.ApplicationPage"
             Background="White">
    
    <UserControl.Styles>
        <Style Selector="Button.action-btn">
            <Setter Property="Background" Value="#9C27B0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style Selector="Button.action-btn:pointerover">
            <Setter Property="Background" Value="#7B1FA2"/>
        </Style>
        <Style Selector="ListBoxItem:selected /template/ ContentPresenter">
            <Setter Property="Background" Value="#E1BEE7"/>
        </Style>
    </UserControl.Styles>
    
    <Grid RowDefinitions="Auto,*">
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="12,8" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                    <TextBlock Text="💾" FontSize="20" VerticalAlignment="Center"/>
                    <TextBlock Text="{DynamicResource DevTools.Tool.Application.Title}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8">
                    <Button Classes="action-btn"
                            Click="CaptureStorageFromWebView"
                            ToolTip.Tip="{DynamicResource DevTools.Application.CaptureStorage.Tooltip}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="🔄" FontSize="14"/>
                            <TextBlock Text="{DynamicResource DevTools.Application.CaptureStorage}"/>
                        </StackPanel>
                    </Button>
                    <Button Classes="action-btn"
                            Click="ClearAllStorage"
                            ToolTip.Tip="{DynamicResource DevTools.Application.ClearStorage.Tooltip}">
                        <StackPanel Orientation="Horizontal" Spacing="4">
                            <TextBlock Text="🗑️" FontSize="14"/>
                            <TextBlock Text="{DynamicResource Common.Clear}"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Content -->
        <Grid Grid.Row="1" ColumnDefinitions="200,4,*">
            
            <!-- Storage Types -->
            <Border Grid.Column="0" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0" Background="#FAFAFA">
                <Grid RowDefinitions="Auto,*">
                    <TextBlock Grid.Row="0" Text="{DynamicResource DevTools.Application.StorageTypes}" FontWeight="SemiBold" Padding="10,8" Background="#F0F0F0"/>
                    <ListBox Grid.Row="1" x:Name="StorageTypesList" SelectionChanged="OnStorageTypeSelected">
                        <ListBoxItem Tag="localStorage">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="📦"/>
                                <TextBlock Text="{DynamicResource DevTools.Application.LocalStorage}"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Tag="sessionStorage">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="📋"/>
                                <TextBlock Text="{DynamicResource DevTools.Application.SessionStorage}"/>
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Tag="cookies">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <TextBlock Text="🍪"/>
                                <TextBlock Text="{DynamicResource DevTools.Application.Cookies}"/>
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </Grid>
            </Border>
            
            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Background="#E0E0E0" ResizeDirection="Columns"/>
            
            <!-- Storage Data -->
            <Grid Grid.Column="2" RowDefinitions="Auto,*">
                <Border Grid.Row="0" Background="#F0F0F0" Padding="10,6" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
                    <TextBlock x:Name="StorageTypeTitle" Text="{DynamicResource DevTools.Application.SelectStorageType}" FontWeight="SemiBold"/>
                </Border>
                
                <ScrollViewer Grid.Row="1" Background="White">
                    <ListBox x:Name="StorageDataList"
                             Background="White"
                             BorderThickness="0"
                             Padding="10">
                    </ListBox>
                </ScrollViewer>
            </Grid>
            
        </Grid>
        
    </Grid>
</UserControl>

